{% extends 'base.html.twig' %}

{% block body %}
    <form method="post" action="{{ path('voiture_save') }}"  class="form-inline">
    	<div class="form-group">
    		<label>Nom : </label>
    		<input type="text" class="form-control" name="nom" required>
    	</div>
    	<div class="form-group">
    		<label>Prénom : </label>
    		<input type="password"class="form-control" name="mp" required>
    	</div>
    	<input type="submit" class="btn btn-success" value="Enregistrer">
    </form>
    <hr>
    <table class="table table-striped table-bordered">
    	<thead>
    		<th>ID</th>
    		<th>NOM</th>
    		<th>MP</th>
            <th></th>
    	</thead>
    	<tbody>
    		{% for listes in liste %}
    			<tr>
    		    	<td><a href="{{ path('voiture_edit',{id:listes.id}) }}">{{listes.id}}</a></td>
    		    	<td>{{listes.nom}}</td>
    		    	<td>{{listes.password}}</td>
                    <td><a href="{{ path('voiture_edit',{id:listes.id}) }}"><button class="btn btn-warning">Modifier</button></a> <a href="{{ path('voiture_delete',{id:listes.id}) }}"><button class="btn btn-danger">Supprimer</button></a></td>
    			</tr>
    		{% endfor %}
    	</tbody>
    </table>
{% endblock %}
